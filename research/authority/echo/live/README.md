# ECHO live method authority

The authoritative method is the `Proposed Method` tab of Google Doc `17iqZWA0bF5unbyuVYnRiW1IUcr0Ctb2KFw1f5XE2poE`, locked at the revision recorded in `source_metadata.json`.

The normalized text export was isolated from the full document between the `III. Proposed Effective Completion via Hybrid Offloading Framework (ECHO)` and `IV. Experimental Evaluation` headings. Its checksum is recorded in `SHA256SUMS`.

Implementation work must preserve the locked physical simulator and implement the documented ECHO delta: end-to-end completion estimation, deterministic ERT queue ordering, deadline-valid masking, minimum-lateness fallback, event-driven SMDP interval rewards, and masked Dueling Double-DQL.

Digitized HOODIE figures are calibration and reproduction references only. They must not be transformed into claimed ECHO simulation measurements.
