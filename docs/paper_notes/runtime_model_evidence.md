# Runtime Model Evidence Matrix

This matrix maps each shared runtime rule or parameter to the best currently staged evidence.
If an exact paper reference is not recoverable, that is stated explicitly instead of guessed.

| Rule / Parameter | Evidence level | Paper reference | OCR support | Code location(s) | Confidence | Status / note |
|---|---|---|---|---|---|---|
| Slot-based progression | OCR-supported | No exact section/subsection/figure recoverable from staged resources | OCR: "discrete slot phases" and "queue admission before reward emission" | `src/environment/slot_engine.py::run_slot()`, `src/environment/slot_engine.py::slot_flow_names()`, `src/environment/runtime_model.py::advance_shared_runtime()` | medium | Fully implemented as slot-scoped control flow, but the exact paper citation is still unresolved. |
| FIFO queue ordering | OCR-supported | No exact section/subsection/table recoverable from staged resources | OCR: queue-phase description preserving FIFO admission/service order | `src/environment/private_queue.py::enqueue()`, `src/environment/private_queue.py::dequeue()`, `src/environment/offloading_queue.py::enqueue()`, `src/environment/public_queue.py::enqueue()` | medium | Fully recovered in code structure; exact paper citation still unresolved. |
| Deadline-based terminal resolution | paper-implied | No exact equation/table recoverable from staged resources | OCR: terminal resolution after execution progression and before reward emission | `src/environment/runtime_model.py::resolve_runtime_terminal_state()`, `src/environment/slot_engine.py::completion_drop_handling()`, `src/environment/environment.py::finalize_task_runtime_state_with_parameters()` | medium | Fully recovered in shared runtime logic, but the paper reference is indirect rather than exact. |
| One-hop offloading progression | paper-implied | No exact section/figure recoverable from staged resources | OCR: "horizontal offloading" and "vertical offloading" with queue-to-queue flow; no exact one-hop sentence recovered | `src/environment/runtime_model.py::advance_shared_runtime()`, `src/environment/slot_engine.py::admit_offload_completion()`, `src/evaluation/runner.py::_resolved_destination()` | medium | Partially recovered; one-hop behavior is enforced, but the exact paper reference remains unresolved. |
| Destination-specific execution timing | paper-implied | No exact equation/section recoverable from staged resources | OCR: Table 4 shows distinct private/public/cloud CPU frequencies, and the text contrasts local vs horizontal vs vertical processing; no exact delay equation recovered | `src/environment/runtime_model.py::compute_service_delay()`, `src/environment/runtime_model.py::advance_shared_runtime()` | medium | Partially recovered; timing behavior is shared and destination-aware, but the source citation is not exact yet. |
| Slot duration | paper-explicit | Table 4, "Time slot duration" = `Δ = 0.1 sec` | OCR: Table 4 lists "Time slot duration" and the value `0.1 sec` | `src/environment/runtime_model.py::SharedRuntimeParameters`, `configs/runtime_model.yml` | high | Fully recovered from Table 4; controls the slot-length multiplier in `compute_service_delay()`. |
| Local service capacity | paper-explicit | Table 4, "CPU frequency in private queues" = `5 GHz` | OCR: Table 4 lists private-queue CPU frequency as `5 GHz` | `src/environment/runtime_model.py::SharedRuntimeParameters`, `src/environment/runtime_model.py::compute_service_delay()`, `configs/runtime_model.yml` | high | Fully recovered from Table 4; controls local-path service delay in `compute_service_delay()`. |
| Public service capacity | paper-explicit | Table 4, "CPU frequency in public queues" = `5 GHz` | OCR: Table 4 lists public-queue CPU frequency as `5 GHz` | `src/environment/runtime_model.py::SharedRuntimeParameters`, `src/environment/runtime_model.py::compute_service_delay()`, `configs/runtime_model.yml` | high | Fully recovered from Table 4; controls public-queue service delay in `compute_service_delay()`. |
| Cloud service capacity | paper-explicit | Table 4, "CPU frequency in Cloud" = `30 GHz` | OCR: Table 4 lists cloud CPU frequency as `30 GHz` | `src/environment/runtime_model.py::SharedRuntimeParameters`, `src/environment/runtime_model.py::compute_service_delay()`, `configs/runtime_model.yml` | high | Fully recovered from Table 4; controls cloud-path service delay in `compute_service_delay()`. |
| Task timeout definition | paper-explicit | Table 4, "Task timeout" = `20 time slots (i.e. 2 sec)` | OCR: Table 4 lists "Task timeout" as `20 time slots (i.e. 2 sec)` | `src/environment/trace_protocol.py::build_deterministic_trace()`, `src/environment/task.py::absolute_deadline_slot`, `src/environment/deadline_rules.py::has_expired()` | high | Fully recovered from Table 4; controls the absolute-deadline calculation used by task creation and deadline checks. |
| Timeout grace behavior | assumption-backed | No exact paper reference recoverable from staged resources | Weak OCR support only | `src/environment/runtime_model.py::resolve_runtime_terminal_state()`, `docs/assumptions/hoodie_assumptions.md::A-010` | low | Assumption-backed via `A-010`; controls the extra deadline allowance in `resolve_runtime_terminal_state()`. |

## Traceability Notes

- Fully traceable today: slot-based progression, FIFO queue ordering, shared deadline resolution, and
  the presence of config-backed runtime parameters in the shared runtime module.
- Partially traceable today: one-hop offloading progression and destination-specific execution timing; both still lack exact paper equation/figure anchors.
- Still lacking exact paper references: timeout grace behavior remains assumption-backed; all other
  currently modeled numeric runtime parameters are recovered from Table 4.
