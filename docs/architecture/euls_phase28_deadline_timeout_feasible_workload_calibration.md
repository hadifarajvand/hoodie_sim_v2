# EULS Phase 28 - Deadline/Timeout Feasible Workload Calibration

This feature introduces an explicit calibration profile for the workload and deadline envelope.

The calibration is diagnostic and preparatory. It does not change reward semantics, policy behavior, or the learned state representation.

The calibrated profile is named `paper_aligned_feasible_v1`.

