# EULS Boundary

Phase 1 introduces an explicit EULS boundary over the existing HOODIE runtime.
It is a wrapper only. It does not change queue semantics, timing semantics,
deadline/drop handling, policy behavior, or training behavior.
