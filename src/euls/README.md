# EULS Boundary

Phase 1 creates an explicit EULS boundary around the existing
`HoodieGymEnvironment` runtime. It is wrapper-only and does not change queue,
timing, deadline/drop, policy, or training semantics.
