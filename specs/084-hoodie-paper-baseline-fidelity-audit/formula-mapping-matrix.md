# Formula Mapping Matrix: HOODIE Paper to Repository Implementation

Status values: `exact`, `approximate`, `missing`, `wrong`, `out_of_scope`.

This matrix is a required audit artifact. It must be completed during implementation by reading the HOODIE paper OCR/PDF and cross-checking the implementation.

| Paper Item | Meaning | Expected Simulation Logic | Current Code Location | Status | Required Fix | Evidence |
|---|---|---|---|---|---|---|
| Paper proposed method: HOODIE | Hybrid computation offloading via distributed DRL | Active proposed method must be exactly `HOODIE` | `src/analysis/hoodie_proposed_method/`; runtime policy registry | approximate | Verify policy identity and ensure no duplicate `HOODIE_PROPOSED` / `ORIGINAL_HOODIE_BASELINE` active methods. | Feature 083 defect context |
| Baseline RO | Random Offloader | Randomly select valid offloading destination/action under same scenario constraints | `src/analysis/hoodie_runtime_evaluation_runner/policies.py` | approximate | Verify randomization is deterministic under benchmark seed and paper-compatible. | Requires code audit |
| Baseline FLC | Full Local Computing | Execute all tasks locally when possible | `src/analysis/hoodie_runtime_evaluation_runner/policies.py` | approximate | Verify no offloading path is selected and local timing/drop behavior matches paper assumptions. | Requires code audit |
| Baseline VO | Vertical Offloader | Prefer vertical/cloud/public offloading path according to paper baseline definition | `src/analysis/hoodie_runtime_evaluation_runner/policies.py` | approximate | Verify vertical path timing includes transmission, queueing, and remote/cloud execution as implemented. | Requires code audit |
| Baseline HO | Horizontal Offloader | Prefer horizontal edge-to-edge offloading path according to paper baseline definition | `src/analysis/hoodie_runtime_evaluation_runner/policies.py` | approximate | Verify horizontal path timing includes transmission, queueing, and remote edge execution as implemented. | Requires code audit |
| Baseline BCO | Balanced Cyclic Offloader | Cycle/balance candidate destinations according to paper baseline definition | `src/analysis/hoodie_runtime_evaluation_runner/policies.py` | approximate | Verify balancing state and cyclic behavior are not accidentally traffic- or queue-biased unless paper requires it. | Requires code audit |
| Baseline MLEO | Minimum Latency Estimation Offloader | Select destination/action with minimum estimated end-to-end completion latency | likely current MQO implementation location in `policies.py` | wrong | Replace active `MQO` / queue-only logic with `MLEO` latency-estimation logic. | Feature 083 currently reports `MQO`, which is not paper-correct. |
| Task completion delay | Primary paper metric | Compute completed task delay consistently from lifecycle timestamps | `metrics.py`, `runner.py`, `model.py` | approximate | Verify exact denominator, completed-task filtering, and timeout/drop interaction. | Requires paper/code audit |
| Task drop ratio | Primary paper metric | dropped tasks / generated or admitted tasks according to paper definition | `metrics.py`, `runner.py` | approximate | Verify denominator matches paper and dropped/timeout semantics are correct. | Requires paper/code audit |
| Reward / cost model | DRL optimization signal | Penalize delay/drop/cost according to paper reward equations | `src/analysis/hoodie_proposed_method/reward_cost_model*`; runtime metrics/report | approximate | Verify exact formula coefficients, terminal penalties, and sign convention. | Requires paper/code audit |
| Local execution delay | Local compute timing | Compute local task execution using task demand and device compute capacity | proposed-method timing modules; runtime model | approximate | Verify units, CPU cycles, capacity, and queue interaction. | Requires paper/code audit |
| Vertical offloading delay | Offload to public/cloud path | Include upload/transmission, queueing, remote execution, and any return/processing components required by paper | offloading/public queue timing modules; runtime model | approximate | Verify full path components and capacity sharing. | Requires paper/code audit |
| Horizontal offloading delay | Edge-to-edge offload path | Include horizontal transmission, queueing, remote edge execution, and deadline/drop semantics | offloading/private queue timing modules; runtime model | approximate | Verify neighbor/action selection and queue semantics. | Requires paper/code audit |
| Private queue timing | Local/private queue behavior | Queue tasks and compute waiting/execution delays per paper model | `private_queue_timing` component | approximate | Verify queue discipline, capacity, and timeout behavior. | Requires paper/code audit |
| Offloading queue timing | Queue for offloaded tasks | Model waiting/processing for offload path | `offloading_queue_timing` component | approximate | Verify relation to vertical/horizontal paths. | Requires paper/code audit |
| Public queue timing | Public/cloud queue behavior | Model public/cloud queue delays and capacity constraints | `public_queue_timing` component | approximate | Verify shared capacity and cloud timing assumptions. | Requires paper/code audit |
| DQN interface | Agent action-value mechanism | Provide Q-value policy interface and training/inference boundary | `dqn_interface` component | approximate | Mark as interface unless real trained DQN is wired and validated. | Feature 080 claim boundary |
| Double-DQN target rule | Target update logic | Use DDQN target selection/evaluation separation if trained implementation exists | `double_dqn_target_rule` component | approximate | Verify whether this is executable training logic or deterministic contract only. | Feature 080 claim boundary |
| Dueling-DQN interface | Value/advantage architecture | Separate value and advantage streams if implemented | `dueling_dqn_value_advantage_interface` component | approximate | Mark as interface unless trainable architecture is actually wired. | Feature 080 claim boundary |
| LSTM recovery/forecast | Traffic/queue forecast or recovery support | Use LSTM-based prediction/recovery if paper requires it | `lstm_forecast_recovery_interface` component | approximate | Do not claim trained LSTM unless actual trained LSTM model exists. | Feature 080 claim boundary |
| Replay memory | DRL training buffer | Store and sample transitions for training | `replay_memory_interface` component | approximate | Verify executable training integration. | Requires code audit |
| Epsilon-greedy schedule | Exploration policy | Decay epsilon during training; epsilon zero in inference | `epsilon_greedy_training_schedule`; `inference_mode_epsilon_zero` | approximate | Verify training/inference split and artifact evidence. | Requires code audit |
| Paper figures/results | Empirical output reproduction | Reproduce paper plots/scenarios only if same simulation setup/training/results exist | artifacts/reporting modules | missing | Add explicit unsupported/approximate status unless full figure reproduction is implemented. | Requires paper/code audit |
