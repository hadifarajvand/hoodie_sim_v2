[tool.pytest.ini_options]
testpaths = ["tests_supported/hoodie"]
norecursedirs = [
  ".git",
  ".venv",
  "venv",
  "build",
  "dist",
  "artifacts",
  "resources",
  "tests_historical",
  "tests",
]
addopts = "-ra"
